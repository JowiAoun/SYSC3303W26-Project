/**
 * DroneState.java
 *
 * Represents the various states a drone can be in.
 */
public enum DroneState {
    IDLE,
    EN_ROUTE,
    EXTINGUISHING,
    RETURNING,
    REFILLING,
    FAULTED // for future iterations
}
